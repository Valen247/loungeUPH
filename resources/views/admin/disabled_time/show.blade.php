@extends('layouts.app')

@section('title','Disabled Time')

@section('content')
    <div class="container">
        <div class="row">

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">disabled_time {{ $disabled_time->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/admin/disabled_time') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/admin/disabled_time/' . $disabled_time->id . '/edit') }}" title="Edit disabled_time"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('admin/disabled_time' . '/' . $disabled_time->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete disabled_time" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $disabled_time->id }}</td>
                                    </tr>
                                    <tr><th> Id Location Time </th><td> {{ $disabled_time->id_location_time }} </td></tr><tr><th> Date </th><td> {{ $disabled_time->date }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
